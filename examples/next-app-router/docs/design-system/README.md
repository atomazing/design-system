# Starter Design-System Docs

These files document how this starter integrates with
`@atomazing-org/design-system`.

- `contract-v2.md`: the current starter integration contract.
- `migration-v2.md`: how to fork or adapt this starter into a real product app.

Generic migration routes and package-level migration rules still live under the
repository root `migrations/` tree.
